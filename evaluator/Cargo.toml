[package]
name = "evaluator"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[dependencies]
analyzer = { path = "../analyzer" }
builtin_fn = { path = "../builtin_fn" }

[build-dependencies]
builtin_fn = { path = "../builtin_fn" }
